/** Bucket names available for file storage */
export const enum Buckets {
    /** Stores the employee documents */
    DocumentosEmpleados = "documentos_empleados",
    /** Stores the service report images and signatures */
    ImagenesServicios = "imagenes_servicios",
    /** Stores the error report captures */
    ReportesError = "reportes_error"
}